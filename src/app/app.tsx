import React from 'react';

// Minimal framework shell for staged commits
export const App = () => (
  <div className="container" style={{ paddingTop: '2rem' }}>
    <h1>Hello MP2</h1>
    <p>Minimal scaffold.</p>
  </div>
);